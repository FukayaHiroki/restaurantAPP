=render partial: 'restaurants/navbar'
.container
  - if best_restaurant.present?
    .card.text-center.m-2
      .card-body
        %h2.card-title.display-4 #{best_restaurant.name}
        .col
          .btn.btn-primary.col-5
            =link_to restaurant_path(best_restaurant.id) do
              詳細ページ
  - else
    #font-size-20.text-center
      %p 現在、NO1レストラは登録されていません。
.container
  .card.col-6.mx-auto
    %ul.list-group.list-group-flush.text-center
      %li.list-group-item
        .btn.btn-primary.col
          = link_to new_restaurant_path do
            投稿する

      %li.list-group-item 
        %button.col.btn.btn-danger{"data-target" => ".bd-example-modal-lg", "data-toggle" => "modal"}
          ログアウト
      %li.list-group-item
        .btn.btn-success.col
          = link_to user_path(current_user.id) do
            投稿した一覧、編集、削除
      %li.list-group-item
        .btn.btn-info.col
          = link_to user_registration_path, method: :PATCH do
            ユーザー情報の変更、削除
      %li.list-group-item
        .btn.btn-dark.col
          = link_to favorites_path do
            お気に入り一覧表示
=render partial: 'restaurants/footer'
=render 'modal'